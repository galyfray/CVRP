import {render} from "@testing-library/react";
import App from "../App";

test("the application pages", () => {
    render(<App />);
});

/**
 * Test("renders prompt to user", () => {
    expect(1).toBe(1);
});


 */